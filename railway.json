{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "preDeployCommand": "php artisan optimize:clear && php artisan migrate --path=database/migrations/2026_07_12_000000_create_distribution_systems_table.php --force && php artisan db:seed --class=AdminUserSeeder --force",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
